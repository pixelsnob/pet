
<div id="cart">
    <form action="<?php echo $this->serverUrl($this->url()); ?>" method="post" name="cart">
        <fieldset class="account">
            <legend>Shopping Cart</legend>
        </fieldset>
        <?php echo $this->flashMessenger($this->use_current_messages); ?>
        <?php if (!empty($this->cart->products)):  ?>
            <div class="items form">
                <ul> 
                    <?php $c = 1; foreach ($this->cart->products as $product): ?>
                    <li class="item<?php echo ($c == count($this->cart->products) ? ' last' : ''); ?>">
                        <div class="cart-left">
                            <h4><?php echo $this->escape($product->name) . ($product->gift ? ' (gift)' : ''); ?></h4>
                            <?php
                            $qty = $this->cart_form->qty->getElement($product->product_id);
                            if ($qty) {
                                echo $qty;
                            }
                            ?>
                            <ul class="links">
                                <li>
                                </li>
                                <li>
                                    <a href="<?php echo $this->serverUrl($this->url(array('product_id' => $product->product_id), 'cart_remove')); ?>" class="remove">Remove</a>
                                </li>
                            </ul>
                        </div>
                        <div class="cart-right">
                            
                        </div>
                    </li>
                    <?php $c++; endforeach; ?>
                </ul>
            </div>
            <p class="submit"><input type="submit" value="Update"></p>
        <?php else: ?>
            <p class="no-items">You have no items in your cart</p> 
        <?php endif; ?>
    </form>

    <!--
    <pre style="font-size:9px;line-height:12px;">
    <?php /*print_r($this->cart); print_r($this->cart->getTotals());*/ ?>
    </pre>
    -->
</div>
