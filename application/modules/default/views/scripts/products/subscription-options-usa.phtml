
<div id="products-renewal-options">
    <?php if ($this->is_renewal): ?>
        <h2>Renewal Options</h2>
        <p>Keep your subscription current &mdash; if you have a promo code, please enter it at checkout!</p>
    <?php else: ?>
        <h2>Subscription Options</h2>
        <p>A 1 year subscription is $59, or save $20 with a 2 year subscription!</p>
    <?php endif; ?>
    <ul class="buttons-list">
        <?php foreach ($this->subscriptions as $sub): ?>
            <li>
                <a href="<?php echo $this->serverUrl($this->url(array('product_id' => $sub->product_id, 'is_gift' => $this->is_gift, 'is_renewal' => $this->is_renewal, 'promo_code' => $this->promo_code), 'cart_add')); ?>" class="button-grad cart-add"><?php echo $this->escape($sub->name); ?></a>
            </li>
        <?php endforeach; ?>
    </ul>
</div>
